{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "format": "prettier --write ./"
  },
  "devDependencies": {
    "nuxt": "^3.3.1",
    "prettier": "^2.8.6"
  },
  "dependencies": {
    "date-fns": "^2.29.3",
    "sass": "^1.59.3"
  }
}
